#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int main(void)
{
	int;n	

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	if (n>)
	{
		printf("is positive")
	}
	else if
	{
		printf ("is zero")
	}
	else (n<0)
	{
		print ("is negative")
	}

	return (0);
}
