#include "main.h"

/**
 * mul - multiplies two integers
 * @a: this character multiplies
 * @b: this character multiplies
 * Return: Always 0
 */

int mul(int a, int b)

{
	for ((int a) mul ( int b))
		printf(a);
			printf(b);
}
