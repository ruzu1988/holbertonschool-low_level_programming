#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _strdup - funtion returns a pointer to a new string
 * @str: the souce string
 * free:
 * Return: NULL
 */

char *_strdup(char *str)
{
	char *dupli;
	int longnom;

	if (dupli == NULL)
		return (NULL);

		dupli = malloc((longnom)) * sizeof(char))
		dupli = *str;

	if (str == NULL)
		return (NULL);

		free(dupli);

	return (0);
}

