#include <stdio.h>

/**
 * main - value
 * @argc: value
 * @argv: varibale
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; argv == 0; i++)
	{
		printf("%s\n", argc[argv]);
	}
	return (0);
}
